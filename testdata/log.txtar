! exec maru2 -l dne
stderr 'ERRO invalid level: "dne"'

exec maru2 --log-level warn
cmp stderr ''
cmp stdout stdout.txt

-- stdout.txt --
default
-- tasks.yaml --
schema-version: v0
tasks:
  default:
    - run: echo "default"
