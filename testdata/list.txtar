exec maru2 --list
cmp stderr stderr1.txt

exec rm tasks.yaml
exec touch tasks.yaml
! exec maru2 --list
cmp stderr stderr2.txt

-- tasks.yaml --
schema-version: v0
tasks:
  default:
    - run: echo "default"

  hello-world:
    - run: echo "Hello, World!"

  another-task:
    - run: echo "Another task"
-- stderr1.txt --
Available:

- default
- another-task
- hello-world
-- stderr2.txt --

ERRO failed to fetch "file:tasks.yaml": unsupported schema version: ""
