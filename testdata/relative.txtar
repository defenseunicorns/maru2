exec maru2 -l debug
stdout 'default\ndefault\ncallback\n'

-- tasks.yaml --
tasks:
  default:
    - run: echo "default"
    - uses: file:dir/maru2-other.yaml

  callback:
    - run: echo "callback"

-- dir/maru2-other.yaml --
tasks:
  default:
    - run: echo "default"
    - uses: file:../tasks.yaml?task=callback
