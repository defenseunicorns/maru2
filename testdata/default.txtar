! exec maru2
cmp stderr stderr.txt

exec maru2 -f maru2-other.yaml
stdout default
stderr 'echo "default"'

mv maru2-other.yaml tasks.yaml

exec maru2
stdout default
stderr 'echo "default"'

exec maru2 default
stdout default
stderr 'echo "default"'

-- maru2-other.yaml --
schema-version: v0
tasks:
  default:
    - run: echo "default"
-- stderr.txt --

ERRO failed to fetch "file:tasks.yaml": stat tasks.yaml: no such file or directory
