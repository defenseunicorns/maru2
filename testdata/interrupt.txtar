! exec maru2 sleep &long&
exec sleep 2
kill -INT long
wait
cmp stderr stderr.txt

-- tasks.yaml --
tasks:
  sleep:
    - run: sleep 10
-- stderr.txt --
$ sleep 10

ERRO task was cancelled
ERRO signal: killed
ERRO at sleep[0] (file:tasks.yaml)
